/**
 * @fileoverview Diese Datei enthält die Implementierung der User-Klasse,
 * die die Benutzerdaten speichert.
 */

/**
 * @class User
 * @description Diese Klasse speichert die Benutzerdaten, einschließlich der Benutzer-ID, des Vornamens, des Nachnamens und der E-Mail-Adresse.
 */
export class User {
    uid: string;
    firstName: string;
    lastName: string;
    email: string;

    /**
     * @constructor
     * Initialisiert ein neues User-Objekt.
     */
    constructor() {}
}
